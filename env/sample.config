[runtime]
environment="$ENV||development"

[server]
host="$HOST||localhost"
port="$PORT||8443"
certs="$CERTS||./env/certs"
certProvider="$CERT_PROVIDER||auto"

[cors]
debug="$CORS_DEBUG||true"

[docs]
base="$DOC_URL||https://localhost/docs"
